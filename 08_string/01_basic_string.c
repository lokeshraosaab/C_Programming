# include<stdio.h>

int main(){
    char st[7] = {'L', 'o', 'k', 'e', 's', 'h', '\0'};
    char str[7] = "Lokesh" ; // This is shortcut way for initialising strings . 
    // In this case C adds a null character automatically.
    return 0;
}
// These two are the ways for initialising strings. 