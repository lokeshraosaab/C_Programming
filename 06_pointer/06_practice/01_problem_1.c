# include<stdio.h>

int main(){
    int i = 4;
    printf("The Value of the variable i is %d \n", i);
    printf("The Address of the variable i is %u \n", &i);
   // int *ptr;
  //  ptr = &i;
  //  printf("The Value of the variable i is %d \n", *ptr);
 //   printf("The Address of the variable i is %u \n", ptr);
    return 0;
}