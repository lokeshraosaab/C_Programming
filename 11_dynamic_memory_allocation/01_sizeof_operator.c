#include <stdio.h>

int main()
{
    printf("The size of the int on my pc is %d \n", sizeof(int));
    printf("The size of the char on my pc is %d \n", sizeof(char));
    printf("The size of the float on my pc is %d \n", sizeof(float));

    return 0;
}